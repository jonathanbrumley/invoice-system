import React from 'react';
import { Invoice } from '@my/shared';

export interface ViewInvoiceProps {
  invoice: Invoice;
}

export const ViewInvoice = (props: ViewInvoiceProps) => {
  return <></>;
}
